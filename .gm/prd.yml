- id: resume-rwkv-training
  subject: Run full 100k training steps on ccsniff-full-history.ndjson
  status: pending
  description: |
    Execute full training on complete ccsniff history. BLOCKER: ccsniff-full-history.ndjson
    currently has only 66 traces (60 KB) instead of the expected 28,174 traces (31 MB).
    User must re-export: npx ccsniff --json --full > ccsniff-full-history.ndjson
    Then run: cargo run --release -p sttx-cli -- train --ccsniff-from ccsniff-full-history.ndjson --steps 100000 --checkpoint-dir ckpt-full-history --checkpoint-every 1000 --model-repo RWKV/RWKV7-Goose-World3-1.5B-HF
  effort: large
  category: feature
  route_family: execution
  load: 0.8
  failure_modes:
    - training fails on traces
    - memory exceeded
    - checkpoint corruption
  route_fit: examined
  authorization: witnessed
  blocking:
  blockedBy:
    - external: ccsniff-full-history.ndjson must be re-exported (28k traces)
  acceptance:
    - Training completes 100000 steps
    - Checkpoints created at 1000-step intervals
    - Final checkpoint written to ckpt-full-history/checkpoint-final
  edge_cases:
